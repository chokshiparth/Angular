import { PrintMeDirective } from './print-me.directive';

describe('PrintMeDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintMeDirective();
    expect(directive).toBeTruthy();
  });
});
