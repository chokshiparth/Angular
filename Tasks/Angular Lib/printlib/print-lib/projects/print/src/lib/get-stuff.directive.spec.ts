import { GetStuffDirective } from './get-stuff.directive';

describe('GetStuffDirective', () => {
  it('should create an instance', () => {
    const directive = new GetStuffDirective();
    expect(directive).toBeTruthy();
  });
});
